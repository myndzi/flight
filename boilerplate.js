'use strict';

module.exports = function (deps) {
    var Promise = deps.promise,
        knex = deps.knex,
        log = deps.log;
    
    return {
        up: function () {
            
        },
        recover: function () {
            
        },
        down: function () {
            
        }
    };
};
